# Conda environment file for CI and deployment
name: gtbook
channels:
  - conda-forge
dependencies:
  - python-graphviz
  - pandas
  - plotly
  - matplotlib
  - pillow # section 5.3 etc..
  - pytorch
  - nbformat
  - pip
