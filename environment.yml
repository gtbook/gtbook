# Conda environment file for CI and deployment
name: gtbook
channels:
  - conda-forge
dependencies:
  - python-graphviz
  - pandas
  - matplotlib
  - pillow # section 5.3 etc..
  - pytorch
  - nbformat
  - pip
